---
name: 一超物流智能追踪系统 - 2026-04-30 状态快照
description: 当前代码修改完成，待部署云函数验证
type: project
---

## 当前架构
- 微信小程序（原生）+ 微信云开发（云函数 + 云数据库）
- AI识别：Kimi API（moonshot-v1-8k-vision-preview）
- OA状态机：created → measured_priced → admin_confirmed → completed

## 最近修改（2026-04-30）
1. measure页移除货物编辑，只保留实测录入+费用计算
2. detail页支持仓管/管理员编辑货物信息（新增updateInfo云函数action）
3. workflow页业务员视角改为"我的运单"
4. custom-tabBar动态标签（业务员→我的运单，仓管→流程）
5. 管理员登录也收集姓名
6. 修复WXML不支持`?.`语法的问题

## 待部署云函数
- cloudfunctions/shipment-update（新增updateInfo、delete action）
- cloudfunctions/user-login（管理员支持name字段）
- cloudfunctions/shipment-query（myShipments返回更多字段）

## 待验证流程
业务员建单 → 仓管实测定价 → 管理员确认 → 仓管确认送达

## 关键约束
- WXML不支持可选链`?.`、空值合并`??`
- 云函数白名单为空时放通所有用户（上线前必须填写）
- 国内微信生态，云开发免费额度有限

## 已知问题
- role-select/index.js 当前是测试模式（固定手机号13800000000，姓名"虞一超"），正式使用前需恢复为正常登录方式
