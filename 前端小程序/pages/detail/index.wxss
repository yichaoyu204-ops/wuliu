.container {
  background: #f3f5f8;
  min-height: 100vh;
  padding-bottom: calc(170rpx + env(safe-area-inset-bottom));
}

.loading-state {
  text-align: center;
  padding: 200rpx 0;
  font-size: 28rpx;
  color: #999;
}

/* 状态卡片 */
.status-card {
  background: linear-gradient(145deg, #14213d 0%, #243655 100%);
  margin: 24rpx;
  border-radius: 10rpx;
  padding: 36rpx 32rpx;
  display: flex;
  align-items: center;
  color: #fff;
  box-shadow: 0 16rpx 36rpx rgba(20, 33, 61, 0.18);
}

.status-icon {
  width: 82rpx;
  height: 82rpx;
  border-radius: 12rpx;
  background: rgba(252, 163, 17, 0.14);
  text-align: center;
  line-height: 82rpx;
  font-size: 48rpx;
  margin-right: 24rpx;
}

.status-info {
  flex: 1;
  min-width: 0;
}

.status-name {
  display: block;
  font-size: 36rpx;
  font-weight: 800;
  margin-bottom: 8rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.status-desc {
  font-size: 26rpx;
  opacity: 0.85;
  line-height: 1.45;
}

/* 区块 */
.section {
  margin: 0 24rpx 24rpx;
}

.section-title {
  font-size: 30rpx;
  font-weight: 800;
  color: #14213d;
  margin-bottom: 16rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* 信息卡片 */
.info-card, .route-card, .fee-card, .measure-card, .quote-card {
  background: #fff;
  border-radius: 10rpx;
  padding: 24rpx 28rpx;
  box-shadow: 0 10rpx 26rpx rgba(20, 33, 61, 0.05);
  border: 1rpx solid #e7ecf2;
}

.info-row {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  padding: 16rpx 0;
  border-bottom: 1rpx solid #f8f8f8;
  gap: 18rpx;
}

.info-row:last-child {
  border-bottom: none;
}

.info-label {
  font-size: 26rpx;
  color: #667085;
  width: 150rpx;
  flex-shrink: 0;
  margin-right: 0;
  display: block;
  line-height: 1.45;
}

.info-value {
  font-size: 26rpx;
  color: #1d2733;
  text-align: right;
  flex: 1;
  min-width: 0;
  word-break: break-all;
  margin-left: 0;
  display: block;
  line-height: 1.45;
}

.info-value.hint {
  color: #bbb;
}

/* 编辑模式 */
.edit-btn {
  font-size: 26rpx;
  color: #8a4b10;
  padding: 4rpx 16rpx;
  border: 1rpx solid #fca311;
  border-radius: 8rpx;
  flex-shrink: 0;
}

.edit-actions {
  display: flex;
  gap: 16rpx;
  flex-shrink: 0;
}

.edit-action {
  font-size: 26rpx;
  padding: 4rpx 16rpx;
  border-radius: 8rpx;
}

.edit-action.cancel {
  color: #888;
  border: 1rpx solid #ddd;
}

.edit-action.save {
  color: #14213d;
  background: #fca311;
}

.edit-input {
  flex: 1;
  border: 1rpx solid #e0e0e0;
  border-radius: 8rpx;
  padding: 10rpx 16rpx;
  font-size: 26rpx;
  color: #1a1a1a;
  background: #fafafa;
  text-align: right;
  margin-left: 0;
  min-width: 0;
}

.edit-input-group {
  display: flex;
  align-items: center;
  flex: 1;
  justify-content: flex-end;
  margin-left: 0;
  gap: 8rpx;
  min-width: 0;
  flex-wrap: wrap;
}

.edit-input.small {
  width: 112rpx;
  flex: none;
  margin-left: 0;
  text-align: center;
}

.edit-unit {
  font-size: 22rpx;
  color: #888;
  flex-shrink: 0;
}

.radio-label {
  display: inline-flex;
  align-items: center;
  margin-left: 16rpx;
  font-size: 26rpx;
  color: #1a1a1a;
}

.mono {
  font-family: monospace;
}

/* 路线卡片 */
.route-name {
  font-size: 30rpx;
  font-weight: 600;
  color: #14213d;
  margin-bottom: 8rpx;
}

.route-desc {
  font-size: 24rpx;
  color: #888;
  margin-bottom: 20rpx;
}

.warehouse-info {
  background: #f8fafc;
  border-radius: 8rpx;
  padding: 20rpx 24rpx;
  line-height: 1.45;
}

.warehouse-label {
  font-size: 24rpx;
  color: #888;
  margin-right: 16rpx;
}

.warehouse-value {
  font-size: 28rpx;
  color: #14213d;
  font-weight: 500;
}

.warehouse-address {
  display: block;
  font-size: 24rpx;
  color: #999;
  margin-top: 8rpx;
}

/* 时间线 */
.timeline {
  background: #fff;
  border-radius: 10rpx;
  padding: 32rpx 28rpx;
  box-shadow: 0 10rpx 26rpx rgba(20, 33, 61, 0.05);
  border: 1rpx solid #e7ecf2;
}

.timeline-item {
  position: relative;
  padding-left: 48rpx;
  padding-bottom: 40rpx;
}

.timeline-item:last-child {
  padding-bottom: 0;
}

.timeline-dot {
  position: absolute;
  left: 0;
  top: 6rpx;
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  background: #e0e0e0;
  border: 4rpx solid #f5f5f5;
}

.timeline-dot.completed {
  background: #0f766e;
  border-color: #e6f6f3;
}

.timeline-dot.active {
  background: #fca311;
  border-color: #fff2d9;
}

.timeline-line {
  position: absolute;
  left: 10rpx;
  top: 28rpx;
  width: 2rpx;
  height: calc(100% - 20rpx);
  background: #e8e8e8;
}

.timeline-content {
  margin-top: -4rpx;
}

.timeline-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 8rpx;
  gap: 16rpx;
}

.timeline-name {
  font-size: 28rpx;
  color: #14213d;
  font-weight: 500;
  flex: 1;
  min-width: 0;
  line-height: 1.4;
}

.timeline-status {
  font-size: 22rpx;
  padding: 4rpx 12rpx;
  border-radius: 999rpx;
  background: #edf2f7;
  color: #999;
  flex-shrink: 0;
}

.timeline-status.completed {
  background: #e6f6f3;
  color: #0f766e;
}

.timeline-status.active {
  background: #fff2d9;
  color: #8a4b10;
}

.timeline-time {
  font-size: 24rpx;
  color: #888;
  margin-bottom: 4rpx;
}

.timeline-operator {
  font-size: 24rpx;
  color: #888;
  margin-bottom: 4rpx;
}

.timeline-remark {
  font-size: 24rpx;
  color: #666;
  background: #f8fafc;
  padding: 12rpx 16rpx;
  border-radius: 8rpx;
  margin-top: 8rpx;
}

.timeline-photos {
  display: flex;
  gap: 12rpx;
  margin-top: 12rpx;
  flex-wrap: wrap;
}

.timeline-photo {
  width: 140rpx;
  height: 140rpx;
  border-radius: 8rpx;
  object-fit: cover;
}

/* 费用卡片 */
.fee-row {
  display: flex;
  justify-content: space-between;
  padding: 14rpx 0;
  border-bottom: 1rpx solid #f8f8f8;
  gap: 24rpx;
}

.fee-name {
  font-size: 26rpx;
  color: #344054;
  flex: 1;
  min-width: 0;
  line-height: 1.4;
}

.fee-amount {
  font-size: 26rpx;
  color: #14213d;
  font-weight: 500;
  flex-shrink: 0;
}

.fee-divider {
  height: 2rpx;
  background: #e8e8e8;
  margin: 16rpx 0;
}

.fee-total {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.fee-total-label {
  font-size: 28rpx;
  color: #14213d;
  font-weight: 600;
}

.fee-total-amount {
  font-size: 36rpx;
  color: #d92d20;
  font-weight: bold;
}

/* 实测卡片 */
.measure-row {
  display: flex;
  justify-content: space-between;
  padding: 16rpx 0;
  border-bottom: 1rpx solid #f8f8f8;
  gap: 24rpx;
}

.measure-row:last-child {
  border-bottom: none;
}

.measure-label {
  font-size: 26rpx;
  color: #888;
}

.measure-value {
  font-size: 26rpx;
  color: #14213d;
  font-weight: 500;
  text-align: right;
}

.measure-photos {
  margin-top: 20rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid #f8f8f8;
}

.measure-photo-title {
  font-size: 24rpx;
  color: #888;
  margin-bottom: 12rpx;
  display: block;
}

.photo-scroll {
  white-space: nowrap;
  display: flex;
  flex-direction: row;
}

.photo-thumb {
  width: 180rpx;
  height: 180rpx;
  border-radius: 8rpx;
  margin-right: 16rpx;
  flex-shrink: 0;
  object-fit: cover;
}

/* 报价卡片 */
.quote-status {
  font-size: 22rpx;
  padding: 4rpx 12rpx;
  border-radius: 999rpx;
  font-weight: 500;
}

.quote-status.draft {
  color: #888;
  background: #f5f5f5;
}

.quote-status.sent {
  color: #8a4b10;
  background: #fff2d9;
}

.quote-status.confirmed {
  color: #0f766e;
  background: #e6f6f3;
}

.quote-row {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  padding: 16rpx 0;
  border-bottom: 1rpx solid #f8f8f8;
  gap: 24rpx;
}

.quote-left {
  flex: 1;
  min-width: 0;
  margin-right: 0;
}

.quote-name {
  font-size: 28rpx;
  color: #14213d;
}

.quote-detail {
  font-size: 22rpx;
  color: #888;
  margin-top: 4rpx;
  line-height: 1.4;
}

.quote-amount {
  font-size: 28rpx;
  color: #14213d;
  font-weight: 500;
  flex-shrink: 0;
}

.quote-divider {
  height: 2rpx;
  background: #e8e8e8;
  margin: 16rpx 0;
}

.quote-total {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.quote-total-label {
  font-size: 30rpx;
  color: #14213d;
  font-weight: 600;
}

.quote-total-amount {
  font-size: 40rpx;
  color: #d92d20;
  font-weight: bold;
}

.quote-note {
  margin-top: 16rpx;
  padding: 16rpx;
  background: #f8fafc;
  border-radius: 8rpx;
  font-size: 24rpx;
  color: #888;
}

/* 进仓单原件 */
.attachment-card {
  background: #fff;
  border-radius: 10rpx;
  padding: 24rpx;
  box-shadow: 0 10rpx 26rpx rgba(20, 33, 61, 0.05);
  border: 1rpx solid #e7ecf2;
  text-align: center;
}

.attachment-thumb {
  width: 100%;
  height: 360rpx;
  border-radius: 8rpx;
  object-fit: cover;
  background: #f5f5f5;
}

.attachment-hint {
  display: block;
  margin-top: 16rpx;
  font-size: 24rpx;
  color: #888;
}

/* 底部操作 */
.action-bar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 20rpx 32rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  background: #fff;
  border-top: 1rpx solid #f0f0f0;
  box-shadow: 0 -10rpx 28rpx rgba(20, 33, 61, 0.08);
}

.btn-primary {
  background: #fca311;
  color: #14213d;
  text-align: center;
  padding: 28rpx;
  border-radius: 8rpx;
  font-size: 32rpx;
  font-weight: 600;
  box-shadow: 0 10rpx 22rpx rgba(252, 163, 17, 0.22);
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 200rpx 0;
}

.empty-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
}

.empty-text {
  font-size: 30rpx;
  color: #999;
  margin-bottom: 12rpx;
}

.empty-hint {
  font-size: 26rpx;
  color: #bbb;
}
